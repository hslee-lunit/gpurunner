[project]
name = "gpu-util-dummy"
version = "0.1.0"
description = "GPU utility script to keep GPUs busy"
requires-python = ">=3.8"
dependencies = [
    "torch>=2.1.0",
]

[project.scripts]
gpurunner = "gpurunner:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

